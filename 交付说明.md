# D1 带走一片属于你的天空 · 独立交付包

深汕气象天文科普馆 · 第四幕 D1 展项。本文件夹是**可独立运行的完整工程**，不依赖主导航和其它展项。

---

## 一、快速运行

> ## ✨ 推荐：直接双击 `index.html`（或 `双击直接打开.html`）
> 本展项现已优化为**完全自包含离线单文件**：
> - **无需安装 Node.js**
> - **无需运行任何黑窗口批处理脚本**
> - **直接双击根目录的 `index.html`**，即可直接在 Edge / Chrome 等浏览器中全屏/窗口离线流畅体验！
> - 所有星空高清图、抽牌交互、坐标卡生成与二维码离线生成均已 100% 兼容本地直接运行。

### 方式 A：直接双击（最简便、推荐）
直接在文件夹中双击 **`index.html`** 或 **`双击直接打开.html`** 即可立即浏览。

### 方式 B：本地 HTTP 服务（备用）
双击 **`0-双击启动.bat`** —— 它会自动检测并启动本地静态服务器，浏览器跳到 `http://localhost:4185`。

### 方式 C：改代码 / 二次开发
打开 `开发者工具(二次开发)/` 文件夹：
1. 双击 **`2-启动开发模式.bat`**
2. 浏览器打开 `http://localhost:5185`，改 `src/` 下源码会自动热更新

### 方式 D：命令行
```bash
npm install        # 首次
npm run dev        # 开发模式  http://localhost:5185
npm run build      # 重新构建单文件到 index.html 与 dist/
npm run preview    # 预览服务  http://localhost:4185
```

---

## 二、目录结构

```
D1 带走一片属于你的天空/
├── index.html                       ★ 核心入口（直接双击即可打开网页！）
├── 双击直接打开.html                 ★ 快捷入口（同样直接双击即可）
├── 0-双击启动.bat                   备用：启动本地 HTTP 服务器
├── 交付说明.md                      说明文档
├── public/d1/                       展项素材（26 张高清图 + manifest.json）
├── d1/                              资源快捷关联（便于根目录直接读取素材）
├── dist/                            构建产物目录
├── 开发者工具(二次开发)/             二次开发专用脚本
│   ├── 1-安装依赖.bat
│   ├── 2-启动开发模式.bat
│   └── 3-预览构建产物.bat
├── src/                             React 源代码
├── package.json
├── tsconfig.json
└── vite.config.ts
```

---

## 三、素材清单

全部位于 `public/d1/`，由 `manifest.json` 索引（页面启动时 `fetch('/d1/manifest.json')` 读取）：

| 目录 | 数量 | 类别卡 | 内容 |
|---|---|---|---|
| `star/` | 12 张 | ✦ 追随一颗星 | 星座图（按预设星座顺序排列并标注） |
| `sky/` | 12 张 | ☀ 回到某一年的天空 | 年份天空图 2014–2024（按年份升序排列并标注） |
| `shenshan/` | 1 张 | ◈ 定位深汕天空 | 深汕天文台视角星座 |
| `random/` | 1 张 | ◎ 交给随机宇宙 | 深空随机图 |

**命名规则**：文件名格式 `类别_日期+内容.扩展名`，如 `star_猎户座和火星（左上）.JPG`、`sky_20220308银河.jpg`。
`D1Page.tsx` 中的 `parseD1File()` 会从文件名里解析出**年份**（8 位数字）与**名称**，用于排序与标注。

**换图方式**：把新图放进对应目录 → 更新 `public/d1/manifest.json` 里该类别数组 → 若是「追随一颗星」，
需同步在 `D1Page.tsx` 的 `STAR_CATALOG` 中补上 `{ name: '文件名去掉star_前缀', label: '展示标注' }`，
`name` 对不上时会退化为按黄道十二星座顺序循环标注。

---

## 四、交互路径

```
屏保轮播（26 张图 7 秒轮换 / 3 秒交叉淡入）
  └─ 点左下角「✦ 定制你的宇宙坐标卡」→ 4 张类型卡依次铺开
        └─ 点任一类型卡 → 弧形画廊（小丑牌式铺开）
              └─ 点一张牌 → 上屏生成「宇宙坐标卡」+ 二维码
                    └─ 点「⟲ 重新选择」可重来
```
- 无操作 **60 秒**自动回到屏保（`IDLE_TIMEOUT`，`D1Page.tsx` 第 8 行）
- 二维码内容 = 当前页面地址 + `?card=<类别>&img=<图片路径>`
- 坐标卡的「气象编号 / 观测者序号」由图片在列表中的位置推导

---

## 五、相对原项目的改动（交付版）

| 改动 | 说明 |
|---|---|
| 移除「← 返回主导航」按钮 | 原按钮位于屏保界面右上角，跳转主导航 `/`；独立包无主导航，故删除 |
| 去掉 `react-router-dom` 依赖 | 入口改为直接渲染 `D1Page` |
| 二维码地址调整 | 原为 `…/#/exhibits/d1?card=…`（依赖路由），现改为 `…/?card=…`，适配独立包 |
| 独立 `index.html` / 端口 | 开发端口 **5185**，预览端口 **4185**，避免与原项目 5173 冲突 |

除上述外，展项逻辑与视觉与原项目**完全一致**。

---

## 六、注意事项

- **素材体积**：`public/d1/` 约 107 MB（26 张高分辨率原图）。`dist/` 会原样拷贝一份，总体积约 215 MB。
  若部署环境体积受限，可考虑先用 `sharp` 压缩后再投放。
- **Vite 监视器**：`public/` 已在 `vite.config.ts` 的 `server.watch.ignored` 中排除。
  原因是大图被看图软件 / 资源管理器缩略图锁定时会让 Vite 的文件监视器 `EBUSY` 崩溃 — **请勿移除该配置**。
- **原项目遗留文件**：原 `src/routes/d1/data.ts` 为历史遗留文件，`D1Page.tsx` 并未引用（类别定义内联在页面里），
  故未纳入本交付包。
